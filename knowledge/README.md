# Canonical knowledge location

SPDX-License-Identifier: CC-BY-4.0

The one editable OKF v0.2 bundle is [skills/health-checkup/references/okf](../skills/health-checkup/references/okf/index.md).

It is nested inside the installable skill so a copy-only Agent Skills installer retains every knowledge dependency. This directory is a navigation pointer, not a duplicate bundle. There are no repository-external runtime links or symlinks. If several skills later share concepts, introduce a deliberate packaging step rather than divergent hand-edited copies.
